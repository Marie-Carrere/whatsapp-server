scalar Date
scalar URL

type Message {
  id: ID!
  content: String!
  createAt: Date!
}

type Chat {
  id: ID!
  name: String!
  picture: URL
  lastMessage: Message
}

type Query {
  chats: [chat!]!
}